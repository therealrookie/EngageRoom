import { Ch5BaseClassForCliNew } from "../Ch5BaseClassForCliNew";
import { ICh5CliNew } from "../ICh5Cli";
export declare class Ch5UpdateProjectCli extends Ch5BaseClassForCliNew implements ICh5CliNew {
    showOutputMessages: boolean;
    private readonly SHELL_FOLDER;
    private readonly PROJECT_CONFIG_JSON_PATH;
    private readonly VSCODE_SCHEMA_JSON_PATH;
    /**
     * Constructor
     */
    constructor(showOutputMessages?: boolean);
    /**
     * Initialize process
     */
    initialize(): Promise<void>;
    /**
     * Verify input parameters
     */
    verifyInputParams(): Promise<void>;
    /**
     * Check if there are questions to be prompted to the integrator
     */
    checkPromptQuestions(): Promise<void>;
    /**
     * Implement this component's main purpose
     */
    processRequest(): Promise<void>;
    getFolderName(): string;
    private doubleDigit;
    /**
     * Clean up
     */
    cleanUp(): Promise<void>;
    private toBoolean;
}
